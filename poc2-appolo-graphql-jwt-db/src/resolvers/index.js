const Query = require('./query');
const Mutation = require('./mutation');
const Book = require('./book');
const User = require('./user');

module.exports = {
	Query,
	// Sub Type Data Structure
	Book,
	// User,
	Mutation
};
